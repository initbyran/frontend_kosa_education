// 자바스크립트는 ArrayList, entry point(main method) 없다
// 기본 자료 구조 : Array(배열)
const myArray = [1,2,3,4]

// 기본 for 구문 : 기본 for 구문 대신 forEach
myArray.forEach(function(data) {
    console.log('결과는 : '+data) // sysout과 같은 역할
} )

// alert() ; 경고창 출력, browser가 가지고 있는 API
alert('소리없는 아우성')
// blocking 함수 : 확인을 누를 때까지 중단되어있음

// node.js를 이용해서 그냥 JavaScript code를 실행
// 변수 선언
console.log(result);
var result = 100;
// JavaScript 변수 ; hoisting이라는 특이한 성질이 있음
// 변수가 아래있어도 에러가 아님 -> JavaScript는 2개의 phase로 실행됨

